# Clé d'API du gateway Hermes (api_server.key de ~/.hermes/config.yaml).
# Partagée par Open WebUI (OPENAI_API_KEY) et le gateway (API_SERVER_KEY).
# À copier dans .env (jamais committé).
MARIA_API_KEY=change-me-in-prod
